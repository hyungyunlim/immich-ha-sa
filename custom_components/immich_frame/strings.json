{
  "config": {
    "step": {
      "user": {
        "title": "Immich Frame Controller",
        "description": "Open the controller setup page, then enter the pairing code shown there.",
        "data": {
          "controller_url": "Controller URL",
          "pairing_code": "Pairing code",
          "device_id": "Device ID",
          "api_token": "Controller API token (optional fallback)"
        }
      }
    },
    "error": {
      "cannot_connect": "Unable to connect to the frame controller.",
      "invalid_url": "Enter a URL that starts with http:// or https://.",
      "invalid_pairing_code": "The pairing code is invalid or expired.",
      "invalid_auth": "The controller API token is invalid.",
      "required": "This field is required.",
      "unknown": "Unexpected error."
    },
    "abort": {
      "already_configured": "This frame controller is already configured."
    }
  }
}
